# 🤖 SARUS AI LAB ASSISTANT ROBOT DEPENDENCIES
# Complete requirements for Jarvis + Sarus integration

# Core Python libraries
asyncio  # Built-in
logging  # Built-in  
pathlib  # Built-in
dataclasses  # Built-in
typing-extensions>=4.7.0
python-dateutil>=2.8.2
datetime  # Built-in

# AI and Machine Learning
openai>=1.0.0
google-generativeai>=0.3.0
anthropic>=0.8.0
transformers>=4.30.0; platform_machine != "armv7l" and platform_machine != "aarch64"
# PyTorch stack excluded on Raspberry Pi by default (use tflite/onnxruntime instead)
torch>=2.0.0; platform_machine != "armv7l" and platform_machine != "aarch64"
torchvision>=0.15.0; platform_machine != "armv7l" and platform_machine != "aarch64"
torchaudio>=2.0.0; platform_machine != "armv7l" and platform_machine != "aarch64"
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0; platform_machine != "armv7l"
onnxruntime>=1.17.3; platform_machine == "aarch64"
tflite-runtime>=2.11.0; platform_machine == "armv7l"

# Computer Vision and Object Detection
# Use apt: python3-opencv on Raspberry Pi; pip wheel often unavailable on ARM
opencv-python>=4.8.0; platform_machine != "armv7l" and platform_machine != "aarch64"
Pillow>=10.0.0
ultralytics>=8.0.0; platform_machine != "armv7l" and platform_machine != "aarch64"  # YOLOv8
face-recognition>=1.3.0; platform_machine != "armv7l" and platform_machine != "aarch64"
dlib>=19.24.0; platform_machine != "armv7l" and platform_machine != "aarch64"
imageio>=2.31.0
scikit-image>=0.21.0

# Speech Recognition and Text-to-Speech
speechrecognition>=3.10.0
pyttsx3>=2.90
pyaudio>=0.2.11
vosk>=0.3.45
openai-whisper>=20231117; platform_machine != "armv7l" and platform_machine != "aarch64"
soundfile>=0.12.0
librosa>=0.10.0; platform_machine != "armv7l"
pvporcupine>=3.0.2  # Wake word detection (Picovoice)
pydub>=0.25.0

# Web Framework and Dashboard
flask>=2.3.0
flask-socketio>=5.3.0
flask-cors>=4.0.0
jinja2>=3.1.0
werkzeug>=2.3.0

# Hardware Control (Raspberry Pi specific)
RPi.GPIO>=0.7.1; platform_machine == "armv7l"
gpiozero>=1.6.2; platform_machine == "armv7l"
adafruit-circuitpython-dht>=3.7.0; platform_machine == "armv7l"
adafruit-circuitpython-mcp3xxx>=1.4.0; platform_machine == "armv7l"
# Picamera2 should be installed via apt (python3-picamera2)

# Simulation and Physics Engine
pybullet>=3.2.5
gymnasium>=0.29.0
matplotlib>=3.7.0
pygame>=2.5.0

# Database and Data Storage
sqlalchemy>=2.0.0
pandas>=2.0.0
influxdb-client>=1.37.0

# Networking and Communication
requests>=2.31.0
websockets>=11.0.0
aiohttp>=3.8.0
python-socketio>=5.8.0
httpx>=0.25.0,<0.28.0

# Configuration and Environment
python-dotenv>=1.0.0
pyyaml>=6.0.0
toml>=0.10.2

# Testing and Development
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
black>=23.7.0
isort>=5.12.0
mypy>=1.5.0

# Security and Encryption
cryptography>=41.0.0
bcrypt>=4.0.0

# Utilities and UI
click>=8.1.0
rich>=13.5.0
tqdm>=4.66.0
humanize>=4.7.0

# Game Controller Support
xbox360controller>=1.1.0
inputs>=0.5

# IoT and Sensor Integration (Raspberry Pi)
pyserial>=3.5; platform_machine == "armv7l"

# Video Processing
moviepy>=1.0.3

# Optional Cloud Services
boto3>=1.28.0  # AWS
google-cloud-storage>=2.10.0  # Google Cloud

# Development Tools
jupyter>=1.0.0
ipython>=8.14.0
